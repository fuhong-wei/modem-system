# 项目问题复盘与面试准备

> 本文记录「可靠 UDP 文件传输与调制解调系统」在开发、联调、测试过程中踩过的坑，以及对应的排查思路和解决方案。
> 写这份文档有两个目的：一是给自己复盘，二是面试 / GitHub 展示时，用真实的问题证明工程能力。
> 全篇用大白话，重点讲「发生了什么 → 怎么定位 → 怎么解决」，而不是只罗列报错。

## 项目是干什么的（30 秒背景）

用 Python 实现一个基于 **UDP** 的可靠文件传输系统，再叠加一层数字通信的仿真：

- **传输层**：UDP 本身不可靠（会丢包、乱序），我在它上面实现了分块、滑动窗口、超时重传、ACK 确认，让它能可靠地传文件。
- **通信仿真层**：BPSK/QPSK 调制解调、信道编码（重复编码 / 汉明编码）、加噪、误码率（BER）分析。
- **界面层**：用 Streamlit 做的网页界面，三个页签（发送 / 接收 / 误码率分析）。

技术栈：Python 3.10 + Streamlit + NumPy/SciPy/Matplotlib + pytest/ruff/mypy。

---

## 问题清单

### 一、环境配置类

### 问题 1：少装了一个依赖，应用一启动就崩

**现象**：部署时执行 `streamlit run app.py`，浏览器一打开就报 `ModuleNotFoundError: No module named 'scipy'`。

**排查过程**：错误堆栈很直白，`src/visualization.py` 第 7 行 `from scipy import signal` 导入失败。顺藤摸瓜发现：`requirements.txt` 里**写了** scipy，但当前环境里**没有装**。用一个命令确认：

```bash
python -c "import scipy"   # 报 ModuleNotFoundError，坐实
```

**根因**：依赖清单和实际环境不一致。这是部署场景里最常见的一类问题——「代码声明了依赖 ≠ 环境真的装了」。

**解决方案**：`pip install scipy`，装完后应用正常启动。教训是部署前要先做一次「环境检查」：Python 版本、关键依赖是否齐全。

**经验总结**：环境问题要先看错误堆栈定位到「缺哪个包」，再用最小命令复现确认，别瞎猜。

---

### 二、代码 Bug 类

### 问题 2：matplotlib 的格式字符串写错了，画图时报错

**现象**：在「误码率分析 → 综合视图」里画时域波形时，报 `ValueError: 'purple-' is not a valid format string (unrecognized character 'u')`。

**排查过程**：报错指向 `app.py` 的 `ax4.plot(..., "purple-", ...)`。问题出在 matplotlib 的「格式字符串」规则上。

**根因**：matplotlib 的 `plot` 第三个参数（简称 fmt）里，**颜色只能用单字母代码**（`b` 蓝、`g` 绿、`r` 红、`c` 青、`m` 品红、`y` 黄、`k` 黑、`w` 白），不能把完整颜色名和线型连在一起写。`"purple-"`、`"brown-"` 这种「完整颜色名 + 线型」的写法不合法，会被当成格式字符串逐字符解析，解析到 `'u'` 就崩了。

**解决方案**：颜色和线型分开写：

```python
# 错误写法
"purple-", "brown-"

# 正确写法
color="purple", linestyle="-"
color="brown", linestyle="-"
```

全项目搜了一遍，同类问题一共 2 处（`purple-` 和 `brown-`），全部改掉。注意 `"orange"`（裸颜色名、没有线型字符）是合法的，不用改。

**经验总结**：这类「库 API 用法错误」的关键是**读懂库的规则**（哪些是合法格式），而不是记结论。修复后要全项目搜同类写法，一次性扫干净。

---

### 三、网络编程类

### 问题 3：UDP 发送报「远程主机强迫关闭了一个现有的连接」（WinError 10054）

**现象**：在「发送文件」页签上传了一个 7.98MB 的 mp4，点「开始发送」后红框报错：

```
发送失败: [WinError 10054] 远程主机强迫关闭了一个现有的连接。
```

**排查过程**：这个报错很容易误导人——「连接被强制关闭」听起来像是网络断了，但 UDP 是**无连接**的，哪来的「关闭连接」？于是写了最小复现脚本：向一个**没有进程监听**的端口发 UDP 包再 `recvfrom`，结果抛的就是 `ConnectionResetError [WinError 10054]`。坐实了根因。

**根因**：这是 Windows 下 UDP 的一个平台特有行为——向一个没有监听的端口发 UDP 包，内核会回一个 ICMP「端口不可达」，程序下一次 `recvfrom` 就抛 `ConnectionResetError`（WinError 10054）。而我发送端的握手代码里**只捕获了 `socket.timeout`，没有捕获 `ConnectionResetError`**，于是这个异常一路冒泡到最外层，显示成一句吓人的「远程主机强迫关闭连接」。真实含义其实是：**接收端没在监听**。

**解决方案**：在握手循环里单独捕获 `ConnectionResetError`，并给出友好提示：

```python
except ConnectionResetError:
    # 目标端口没有 UDP 监听 = 接收端未启动/已停止，无需重试
    self.on_message("error", "目标端口 X 无监听（接收端未启动或已停止），请先点「开始监听」")
    return False
```

**经验总结**：异常处理要**覆盖平台特有的错误类型**，不能只处理 `timeout` 就完事。另外，「把底层错误翻译成用户能看懂的话」是工程里很重要的一环——`WinError 10054` 对用户毫无意义，「接收端没启动」才有意义。

---

### 问题 4：接收端口被占用，绑定失败

**现象**：接收端点「开始监听」，如果 8888 端口已经被别的进程（或上次残留的接收线程）占了，`socket.bind` 就会失败。

**排查过程**：用 `netstat -ano -p udp | grep :8888` 查 UDP 端口占用。注意要区分 TCP 和 UDP——它们端口号是**独立的命名空间**，TCP 8888 被占用不影响 UDP 8888。

**根因**：`bind(("0.0.0.0", 8888))` 在端口已被占用时抛 `OSError`。原代码没处理，线程静默退出。

**解决方案**：绑定失败时**自动尝试下一个可用端口**（8888 → 8889 → ... 最多试 10 个），并把实际端口通过 `receiver_actual_port` 暴露到界面，提示「端口 8888 被占用，已自动改用 8889 监听」。

**经验总结**：端口这类「运行时资源」很容易冲突，健壮的程序要么检测冲突给出明确提示，要么自动降级到备用端口。

---

### 问题 5：接收端「偷偷」退出了——空闲超时太短

**现象**：用户先点「开始监听」，然后切到发送页签、上传一个 7.98MB 的文件、再点「发送」，结果发送失败。接收端明明点过「开始监听」，但已经不在监听了。

**排查过程**：读接收端代码发现，原来的 `start_receiver` 里有一个「空闲超时」逻辑：`timeout * 3 = 9 秒`没收到任何包就自动 `break` 退出。而「先监听 → 再切页签上传大文件 → 再点发送」这套操作很容易超过 9 秒，等用户点发送时，接收端早就退出了。

**根因**：给接收端设了一个**过短的自动退出时间**，和「先监听、后上传大文件」的真实操作节奏冲突。

**解决方案**：去掉空闲超时，改成「一直监听，直到收到完整文件（END_OF_TRANSMISSION）或用户主动点「停止监听」」。配套把「停止监听」做成真正关闭 socket（见问题 6/8 的线程生命周期管理）。

**经验总结**：定时器 / 超时这类参数，**默认值要贴合真实使用场景**，否则会造成「看起来在跑、其实早死了」的假象，非常难查。

---

### 四、多线程 / 状态管理类

### 问题 6：接收页显示「正在监听」，发送页却说「未监听」（竞态条件）

**现象**：接收页签点「开始监听」后显示 `🔴 正在监听中`，切到发送页签点「发送」却弹「接收端未在监听」。**同一个状态，两个页签读出来的值不一样。**

**排查过程**：这是整个项目里最难查、也最有价值的一个问题。定位过程分三步：

1. **读代码定位状态存哪**：`receiver_alive` 是 `ReliableUDPTransfer` 的一个普通布尔属性（挂在 `st.session_state["transfer"]` 上），接收页和发送页读的其实是**同一个变量**——所以不是「读错变量」的问题。
2. **读运行日志找证据**：Streamlit 日志里全是 `Thread 'Thread-x': missing ScriptRunContext!`，说明接收线程在后台调 `st.*` 是失效的（见问题 8），但**没有 traceback**，说明线程不是崩溃，是「干净退出」。
3. **顺藤摸瓜找到竞态**：这个布尔变量被**两个线程同时写**——主线程在点「开始监听」时乐观地写 `True`，接收线程在 `finally` 里写 `False`。当接收线程启动后立刻失败退出（比如端口占用绑定失败），它的 `finally` 把值改回 `False`，但主线程的乐观 `True` 已经渲染成了「正在监听」。于是「接收页看是 True、发送页读是 False」。

**根因**：**多个线程写同一个共享可变状态，没有同步机制**。这是典型的竞态条件（race condition）。

**解决方案**：分三步把它做成线程安全的：

1. `receiver_alive` 改成 **`threading.Event`**（只读 property 对外暴露），**唯一写者是接收线程**——入口 `set()`、`finally` `clear()`；删除主线程的「乐观置位」。
2. 引入**代际计数器 `_receiver_generation`**：接收线程退出时，只有当「当前代际还是自己」才 `clear()`，避免旧的接收线程退出时误清新线程的状态（解决快速 stop→start 的竞态）。
3. UI 两处都读这一个 Event，不再有第二写者。

**经验总结**：并发 bug 的特点是「时有时无、难复现、现象诡异」。排查的关键是**先想清楚「谁在读、谁在写、在哪个线程」**，再用日志 / 复现脚本把时序坐实。修复的核心原则是**单一写者 + 线程安全结构**，而不是到处加锁。

> 这个问题的详细复盘见 [PROJECT_REVIEW.md](PROJECT_REVIEW.md)。

---

### 问题 7：绑定失败的错误被「吞掉」了

**现象**：接收端绑定失败时，界面上**什么提示都没有**，线程就静默退出了，用户根本不知道发生了什么。

**排查过程**：结合问题 6 的日志发现，接收线程报错用的 `on_message("error", ...)` 在后台线程里执行的是 `st.error(...)`，而 `st.*` 在后台线程是**静默 no-op**（见问题 8），所以错误消息被无声无息地吞掉了。

**根因**：错误上报通道失效——后台线程调 `st.*` 不生效，导致异常被捕获后「上报了个寂寞」。

**解决方案**：新增一个 `receiver_error` 字段，由接收线程**写入**，UI 主线程**读取并显示**（绕开「后台线程直接调 st.*」这条路）。绑定失败、启动异常都会把原因写到这个字段，界面上能看到明确提示。

**经验总结**：「异常被吞掉」比「异常抛出来」可怕得多——抛出来至少能查到，吞掉了就是黑盒。任何 try/except 都要想清楚：**异常被捕获之后，信息到底去哪了？**

---

### 问题 8：后台线程不能直接操作界面（Streamlit 的限制）

**现象**：接收端的进度、状态、日志**从来没在界面上显示过**——点了「开始监听」，进度条永远不动，收文件时也看不到任何提示。

**排查过程**：Streamlit 运行日志里反复出现：

```
Thread 'Thread-5 (receiver_thread)': missing ScriptRunContext!
```

这是决定性证据。

**根因**：Streamlit 的 `st.success / st.error / st.progress` 这些函数，**只能在主线程（有 ScriptRunContext 的上下文）里调用**。而接收端是在一个 `threading.Thread` 后台线程里跑的，它去调 `st.*` 时，Streamlit 检测到「没有脚本运行上下文」，于是打印一条警告然后**直接什么都不做**——既不报错，也不显示。

**解决方案**：让后台线程**彻底不再碰 `st.*`**，改成「后台写缓冲、主线程读缓冲」：

1. 接收线程把消息 / 进度 / 状态写进一个**线程安全的缓冲**（`threading.Lock` 保护）。
2. UI 用 `st.fragment(run_every="1s")` 每秒自动刷新，调 `drain_receiver_events()` 把缓冲里的内容取出来，在**主线程**里渲染。
3. 接收线程产出的数据记录，通过 `set/take_last_received_record()` 线程安全地交给主线程，再写进 `session_state`（后台线程也不直接写 session_state）。

**经验总结**：这是「UI 框架和后台线程」之间的一个通用约束——**绝大多数 GUI/Web 框架都不允许非主线程直接操作界面**。正确姿势是「后台线程只算数据、写缓冲；主线程负责读缓冲、刷界面」。这个模式在 Electron、Qt、Android、Streamlit 里都是相通的。

---

### 五、性能 / 大文件类

### 问题 9：大文件到底传成功没有？（用哈希做最终裁判）

**现象**：测试了多个 10~88MB 的视频文件，肉眼不确定是否真的「完整、无损」传过去了。

**排查过程**：肉眼看不出来，就用**密码学哈希（SHA256）**当裁判——只要发送前和接收后的文件哈希完全一致，就证明逐字节无损。做法：

1. 列出接收目录的文件（`received_C1204.MP4` 等）。
2. 找到对应的原始文件（按文件名 + 大小在电脑上搜，找到后比对大小）。
3. 用 `sha256sum` 计算双方哈希，逐字节比对。

**结论**：

| 文件 | 大小 | 结果 |
| --- | --- | --- |
| C1204.MP4 | 88.1 MB | ✅ 哈希完全一致（bit-perfect） |
| 9c91064….mp4 | 18.6 MB | ✅ 哈希完全一致 |
| 0B9655….jpg | 0.9 MB | ✅ 哈希完全一致 |
| C1203.MP4 | 29.4 MB | ⚠️ 完整接收文件已被清除，仅前 1000 字节样本一致 |
| b49bed2….mp4 | 19.4 MB | ❌ 疑似失败（样本不一致 + 时序异常） |

**最重要的结论**：**最大的 88.1MB 文件哈希逐字节一致**，说明分块 + 滑动窗口 + 超时重传 + ACK 这套可靠传输机制对大文件是**正确且完整**的。b49 那次的样本不一致 + 时间戳异常，很可能对应一次「接收端没在监听就开始发送」或中途被打断的失败传输。

**大文件相关的潜在优化点**（面试可谈）：

1. 发送端 `uploaded_file.getvalue()` 把整个文件读进内存，GB 级文件会吃内存，可改流式分块读取。
2. 发送主循环每次只 `recvfrom` 一个 ACK，窗口 8 时吞吐受限，可改成一次 drain 多个 ACK。
3. 协议层目前靠 UDP 自带校验和 + 重传，可加 CRC32 逐块校验，检测极端情况下的静默损坏。
4. 接收端目前收完一个文件就停止监听，可改成循环监听，一次监听传多个文件。

**经验总结**：**验证系统正确性要用「可证伪」的手段**——哈希比对就是传输类系统的「铁证」。肉眼、感觉、界面提示都不算数。

---

## 面试问答准备

> 针对上面每个问题，预测面试官可能的追问，并给出回答要点。

**Q1（scipy 依赖）**
- 追问：怎么避免「依赖装了但环境不一致」？
- 要点：用虚拟环境 + `requirements.txt` 锁定依赖；部署前做环境自检脚本；CI 里跑一遍 `pip install -r requirements.txt` 验证可安装。

**Q2（matplotlib 格式字符串）**
- 追问：matplotlib 的 fmt 支持哪些颜色写法？
- 要点：单字母颜色码（`b/g/r/c/m/y/k/w`）、`color=` 关键字（完整颜色名 / 十六进制 / RGB 元组）、裸完整颜色名（`"orange"` 合法但 `"purple-"` 非法）。核心是「颜色 + 线型 + 标记」三者的合法组合规则。

**Q3（WinError 10054）**
- 追问：为什么 UDP 无连接还会有「连接重置」？
- 要点：不是 TCP 的连接重置，是 Windows 下 UDP 收到 ICMP「端口不可达」后，内核把这个错误映射成 `recvfrom` 的 `ConnectionResetError(10054)`。这是平台特有行为，Linux 下通常表现为 `ECONNREFUSED` 或超时。所以异常处理要覆盖平台差异。

**Q4（端口占用）**
- 追问：UDP 和 TCP 端口能共用同一个端口号吗？
- 要点：能。UDP 和 TCP 是独立的协议命名空间，`netstat` 查的时候要加 `-p udp` / `-p tcp` 区分。所以「TCP 8888 被占」不影响「UDP 8888」。

**Q5（空闲超时）**
- 追问：定时器超时设多长合适？
- 要点：没有万能值，要看真实使用场景。这里的问题本质是「自动退出」和「用户操作耗时」的冲突——要么延长到明显大于用户操作时间，要么改成「用户显式停止」，别让系统自己猜。

**Q6（竞态条件）**
- 追问：除了 Event，还有哪些线程安全手段？为什么会选 Event + 代际计数？
- 要点：常见手段有 `threading.Lock`（互斥）、`threading.Event`（布尔标志）、`queue.Queue`（生产者消费者）、`Condition`（条件变量）。这里 `receiver_alive` 本质是个「是否在运行」的布尔标志，用 Event 最自然；加代际计数是为了解决「旧线程退出时误清新线程状态」这种**时序竞态**。核心是「单一写者」原则。

**Q7（异常被吞）**
- 追问：怎么保证异常一定被看到？
- 要点：任何 `except` 都要问一句「信息去哪了」。要么上报（日志/UI/监控），要么记录（字段/队列），绝不能只 `pass` 或调一个可能失效的通道。

**Q8（后台线程 vs UI）**
- 追问：除了 Streamlit，还有哪些框架有这个限制？通用解法是什么？
- 要点：几乎所有 GUI/Web 框架都是（Android 的「主线程才能碰 View」、Qt 的信号槽、Electron 的主进程/渲染进程、SwiftUI 的 main actor）。通用解法就一条：**后台线程只算数据写缓冲，主线程负责读缓冲刷界面**。

**Q9（大文件验证）**
- 追问：UDP 可靠传输里，校验和 / 序号 / 重传分别解决什么问题？
- 要点：序号 + ACK 解决「乱序和丢失」；超时重传解决「丢包后的恢复」；滑动窗口解决「吞吐和流控」；校验和（UDP 自带或应用层 CRC）解决「内容损坏」。我这个项目实现了前三者，校验和目前依赖 UDP 自带，可作为后续优化点。

---

## 个人成长总结

回看这九个问题的解决过程，我总结出几条**通用的工程思维**，它们比任何单个 bug 的修法都值钱：

1. **异常处理要「对症」且「可见」**：不能只抓 `timeout` 就完事，要覆盖平台特有错误（如 Windows 的 10054）；抓了异常之后，一定要想清楚信息去哪了——吞掉异常比抛异常可怕得多。

2. **多线程共享状态，先想清楚「谁读、谁写、在哪个线程」**：一旦出现多个写者，就埋下了竞态的雷。核心解法是**单一写者 + 线程安全结构**（Event / Lock / Queue），而不是到处撒锁。

3. **后台线程不能直接操作 UI**：这是跨框架的通用约束。正确姿势是「后台写缓冲、主线程刷界面」。

4. **平台差异和框架限制要提前有意识**：Windows 和 Linux 的 UDP 行为不同，Streamlit 的后台线程有 ScriptRunContext 限制——这些不是「知识」，是「坑」，踩过才知道。

5. **验证系统要用「可证伪」的手段**：传输系统用哈希比对，端口占用用 netstat 查，线程状态看日志里的 traceback/warning。感觉和肉眼都不算数。

6. **排查问题的通用路径**：现象 → 最小复现 → 定位根因 → 修复 → 全项目排查同类问题 → 回归测试。每一步都要有证据，不能靠猜。

---

## 附：关键文件位置

- `src/transport.py`：可靠 UDP 传输、接收端线程生命周期、线程安全缓冲
- `src/modulation.py`：调制解调、信道模拟、误码率
- `src/coding.py`：重复编码 / 汉明编码
- `src/visualization.py`：星座图 / 频谱 / 瀑布图
- `app.py`：Streamlit 界面层、接收端实时面板（`st.fragment`）
- `PROJECT_REVIEW.md`：`receiver_alive` 竞态问题的详细复盘
